# ===========================================
# MicroPlastics Data Entry System
# Environment Configuration Template
# ===========================================
# 复制此文件为项目根目录的 .env 并填入实际值
# Copy this file to .env in project root and fill in actual values
# ===========================================

# ----- 应用配置 / Application Config -----
NODE_ENV=development
PORT=3001

# ----- 数据库配置 / Database Config -----
# 远程服务器数据库 / Remote Server Database
DB_HOST=104.247.77.90
DB_USER=your_database_username
DB_PASS=your_database_password
DB_NAME=sweetl23_partner_demo

# ----- Session配置 / Session Config -----
# 用于加密session的密钥 (生产环境请使用强密钥)
# Secret key for session encryption (use strong key in production)
SESSION_SECRET=your_secure_random_session_secret_key_here
# Session超时时间 (秒) / Session timeout (seconds)
SESSION_TIMEOUT=1800
# Cookie设置 / Cookie settings
COOKIE_HTTP_ONLY=true

# ----- 邮件配置 / SMTP Config -----
# SMTP服务器配置 (用于密码重置和联系表单)
# SMTP server config (for password reset and contact form)
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_USER=your_email@domain.com
SMTP_PASS=your_email_password

# ----- 管理员邮箱 / Admin Emails -----
# 联系表单提交通知接收者
# Contact form submission notification recipients
ADMIN_EMAIL_1=admin1@domain.com
ADMIN_EMAIL_2=admin2@domain.com
ADMIN_EMAIL_3=admin3@domain.com

# ----- 生产环境配置 / Production Config -----
# CORS允许的域名 (逗号分隔)
# Allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS=https://your-production-domain.com,https://www.your-domain.com
